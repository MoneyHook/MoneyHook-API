services:
  go:
    command: ["sh", "-c", "cd /workspace/app && go run ./cmd/migrate && go run ."]
    environment:
      ENABLE_SEED_DATA: "true"
      ENABLE_DEVELOPMENT_USER: "true"
      FRONT_URL: "${PLAYWRIGHT_FRONT_URL:-http://localhost:3000}"
